apiVersion: apps/v1
kind: Deployment
metadata:
  name: stockapp
  labels:
    app: flask-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-app
  template:
    metadata:
      labels:
        app: flask-app
    spec:
      containers:
      - name: flask-app
        image: mohitmatta/flaskapp
        ports:
        - containerPort: 8000
